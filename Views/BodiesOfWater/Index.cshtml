@model IEnumerable<WIShipwrecks.Models.BodiesOfWater>

@{
    ViewBag.Title = "Bodies Of Water";
}

<h2>Bodies Of Water</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BodyOfWater)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
			<a href="@Url.Action("Details", "BodiesOfWater", new { id = item.ID })">
				@Html.DisplayFor(modelItem => item.BodyOfWater)
			</a>
        </td>
        <td width="70px">
			<a href="@Url.Action("Edit", "BodiesOfWater", new { id = item.ID })" class="btn btn-info btn-xs">
				<span class="glyphicon glyphicon-pencil"></span>
            </a>
            <a href="@Url.Action("Delete", "BodiesOfWater", new { id = item.ID })" class="btn btn-danger btn-xs">
				<span class="glyphicon glyphicon-trash"></span>
            </a>
        </td>
    </tr>
}

</table>
