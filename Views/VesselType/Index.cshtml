@model IEnumerable<WIShipwrecks.Models.VesselType>

@{
    ViewBag.Title = "Vessel Types";
}

<h2>Vessel Types</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VesselType1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
			<a href="@Url.Action("Details", "VesselType", new { id = item.ID })">
				@Html.DisplayFor(modelItem => item.VesselType1)
			</a>
        </td>
        <td width="70px">
			<a href="@Url.Action("Edit", "VesselType", new { id = item.ID })" class="btn btn-info btn-xs">
				<span class="glyphicon glyphicon-pencil"></span>
            </a>
            <a href="@Url.Action("Delete", "VesselType", new { id = item.ID })" class="btn btn-danger btn-xs">
				<span class="glyphicon glyphicon-trash"></span>
            </a>
        </td>
    </tr>
}

</table>
