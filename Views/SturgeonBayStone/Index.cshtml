@model IEnumerable<WIShipwrecks.Models.SturgeonBayStone>

@{
    ViewBag.Title = "Sturgeon Bay Stone";
}

<h2>Sturgeon Bay Stone</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SturgeonBayStone1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
			<a href="@Url.Action("Details", "SturgeonBayStone", new { id = item.ID })">
				@Html.DisplayFor(modelItem => item.SturgeonBayStone1)
			</a>
        </td>
        <td width="70px">
			<a href="@Url.Action("Edit", "SturgeonBayStone", new { id = item.ID })" class="btn btn-info btn-xs">
				<span class="glyphicon glyphicon-pencil"></span>
            </a>
            <a href="@Url.Action("Delete", "SturgeonBayStone", new { id = item.ID })" class="btn btn-danger btn-xs">
				<span class="glyphicon glyphicon-trash"></span>
            </a>
        </td>
    </tr>
}

</table>
