@model IEnumerable<WIShipwrecks.Models.RigType>

@{
    ViewBag.Title = "Rig Types";
}

<h2>Rig Types</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RigType1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
			<a href="@Url.Action("Details", "RigType", new { id = item.ID })">
				@Html.DisplayFor(modelItem => item.RigType1)
			</a>
        </td>
        <td width="70px">
			<a href="@Url.Action("Edit", "RigType", new { id = item.ID })" class="btn btn-info btn-xs">
				<span class="glyphicon glyphicon-pencil"></span>
            </a>
            <a href="@Url.Action("Delete", "RigType", new { id = item.ID })" class="btn btn-danger btn-xs">
				<span class="glyphicon glyphicon-trash"></span>
            </a>
        </td>
    </tr>
}

</table>
