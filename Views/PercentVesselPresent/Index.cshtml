@model IEnumerable<WIShipwrecks.Models.PercentVesselPresent>

@{
    ViewBag.Title = "Percentages of Vessel Present";
}

<h2>Percentages of Vessel Present</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Range)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
			<a href="@Url.Action("Details", "PercentVesselPresent", new { id = item.ID })">
				@Html.DisplayFor(modelItem => item.Range)
			</a>
        </td>
        <td width="70px">
			<a href="@Url.Action("Edit", "PercentVesselPresent", new { id = item.ID })" class="btn btn-info btn-xs">
				<span class="glyphicon glyphicon-pencil"></span>
            </a>
            <a href="@Url.Action("Delete", "PercentVesselPresent", new { id = item.ID })" class="btn btn-danger btn-xs">
				<span class="glyphicon glyphicon-trash"></span>
            </a>
        </td>
    </tr>
}

</table>
