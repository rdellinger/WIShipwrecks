@model IEnumerable<WIShipwrecks.Models.CasualtyType>

@{
    ViewBag.Title = "Casualty Types";
}

<h2>Casualty Types</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CasualtyType1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
			<a href="@Url.Action("Details", "CasualtyType", new { id = item.ID })">
				@Html.DisplayFor(modelItem => item.CasualtyType1)
			</a>
        </td>
        <td width="70px">
			<a href="@Url.Action("Edit", "CasualtyType", new { id = item.ID })" class="btn btn-info btn-xs">
				<span class="glyphicon glyphicon-pencil"></span>
            </a>
            <a href="@Url.Action("Delete", "CasualtyType", new { id = item.ID })" class="btn btn-danger btn-xs">
				<span class="glyphicon glyphicon-trash"></span>
            </a>
        </td>
    </tr>
}

</table>
