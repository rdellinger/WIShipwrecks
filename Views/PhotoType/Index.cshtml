@model IEnumerable<WIShipwrecks.Models.PhotoType>

@{
    ViewBag.Title = "Photo Types";
}

<h2>Photo Types</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PhotoType1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
			<a href="@Url.Action("Details", "PhotoType", new { id = item.ID })">
				@Html.DisplayFor(modelItem => item.PhotoType1)
			</a>
        </td>
        <td width="70px">
			<a href="@Url.Action("Edit", "PhotoType", new { id = item.ID })" class="btn btn-info btn-xs">
				<span class="glyphicon glyphicon-pencil"></span>
            </a>
            <a href="@Url.Action("Delete", "PhotoType", new { id = item.ID })" class="btn btn-danger btn-xs">
				<span class="glyphicon glyphicon-trash"></span>
            </a>
        </td>
    </tr>
}

</table>
