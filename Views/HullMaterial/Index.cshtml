@model IEnumerable<WIShipwrecks.Models.HullMaterial>

@{
    ViewBag.Title = "Hull Materials";
}

<h2>Hull Materials</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HullMaterial1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
			<a href="@Url.Action("Details", "HullMaterial", new { id = item.ID })">
				@Html.DisplayFor(modelItem => item.HullMaterial1)
			</a>
        </td>
        <td width="70px">
			<a href="@Url.Action("Edit", "HullMaterial", new { id = item.ID })" class="btn btn-info btn-xs">
				<span class="glyphicon glyphicon-pencil"></span>
            </a>
            <a href="@Url.Action("Delete", "HullMaterial", new { id = item.ID })" class="btn btn-danger btn-xs">
				<span class="glyphicon glyphicon-trash"></span>
            </a>
        </td>
    </tr>
}

</table>
